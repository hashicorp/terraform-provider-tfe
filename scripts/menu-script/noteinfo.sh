#!/bin/bash
echo
read -p "A note or warning can be attached to any schema description using the following syntax: 

\n\n ~> "
echo
read -p "Here is an example from "
echo
read -p "As they are defined in the schema, these notes are tied to a specific description. If you have more general notes go to Main Menu > Custom."
