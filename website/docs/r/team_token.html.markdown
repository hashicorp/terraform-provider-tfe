---
layout: "tfe"
page_title: "Terraform Enterprise: Resource tfe_team_token"
description: |-
  Generates a new team token. If no description is provided, it follows the legacy behavior to override the existing, descriptionless token if one exists.
---

# Resource: tfe_team_token

Generates a new team token. If no description is provided, it follows the legacy behavior to override the existing, descriptionless token if one exists.

## Example Usage

```terraform
# When a token has an expiry

resource "tfe_team" "test" {
  name         = "my-team-name"
  organization = "my-org-name"
}

resource "tfe_team_token" "test" {
  team_id     = tfe_team.test.id
  description = "my team token"
}

resource "tfe_team_token" "ci" {
  team_id     = tfe_team.test.id
  description = "my second team token"
}
```

```terraform
# Basic usage

resource "tfe_team" "test" {
  name         = "my-team-name"
  organization = "my-org-name"
}

resource "time_rotating" "example" {
  rotation_days = 30
}

resource "tfe_team_token" "test" {
  team_id     = tfe_team.test.id
  description = "my team token"
  expired_at  = time_rotating.example.rotation_rfc3339
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `team_id` (String) ID of the team.

### Optional

- `description` (String) The description of the token, which must be unique per team.
- `expired_at` (String) The token's expiration date. The expiration date must be a date/time string in RFC3339 format (e.g., "2024-12-31T23:59:59Z"). If no expiration date is supplied, the token will expire 24 months from creation and a warning during plan and apply phases will be displayed.
- `force_regenerate` (Boolean) Only applies to legacy tokens without descriptions. If set to `true`, a new token will be generated even if a token already exists. This will invalidate the existing token! Regeneration is triggered only when this value changes.

### Read-Only

- `id` (String) The ID of the team token.
- `token` (String, Sensitive) The generated token.



## Import

Resource tfe_team_token can be imported in the following format: 

```shell
# via <TOKEN ID>
terraform import tfe_team_token.test at-47qC3LmA47piVan7

# via <TEAM ID>
terraform import tfe_team_token.test team-47qC3LmA47piVan7
```
