---
layout: "tfe"
page_title: "Terraform Enterprise: Resource tfe_scim_settings"
description: |-
  (Only for Terraform Enterprise) Manages SCIM provisioning settings for the Terraform Enterprise instance.
  Requires admin token configuration. See example usage for incorporating an admin token in your provider config.
  -> Note: SCIM requires SAML to be configured first, so the examples below depend on a tfe_saml_settings resource. While this resource exists, SCIM is always enabled = true; running terraform destroy disables SCIM.
  -> Note: paused and site_admin_group_scim_id are the only mutable arguments. To fully disable SCIM you must run terraform destroy on this resource; there is no argument to disable it in-place.
---


<!-- Please do not edit this file, it is generated. -->
# Resource: tfe_scim_settings

(Only for Terraform Enterprise) Manages SCIM provisioning settings for the Terraform Enterprise instance.

Requires admin token configuration. See example usage for incorporating an admin token in your provider config.

-> **Note:** SCIM requires SAML to be configured first, so the examples below depend on a `tfeSamlSettings` resource. While this resource exists, SCIM is always `enabled = true`; running `terraform destroy` disables SCIM.

-> **Note:** `paused` and `siteAdminGroupScimId` are the only mutable arguments. To fully disable SCIM you must run `terraform destroy` on this resource; there is no argument to disable it in-place.

## Example Usage

```typescript
import * as constructs from "constructs";
import * as cdktf from "cdktf";
/*Provider bindings are generated by running cdktf get.
See https://cdk.tf/provider-generation for more details.*/
import * as tfe from "./.gen/providers/tfe";
class MyConvertedCode extends cdktf.TerraformStack {
  constructor(scope: constructs.Construct, name: string) {
    super(scope, name);
    const tfeSamlSettingsThis = new tfe.samlSettings.SamlSettings(
      this,
      "this",
      {
        idpCert: "foobarCertificate",
        providerType: "okta",
        sloEndpointUrl: "https://example.com/slo_endpoint_url",
        ssoEndpointUrl: "https://example.com/sso_endpoint_url",
      }
    );
    const tfeScimSettingsThis = new tfe.scimSettings.ScimSettings(
      this,
      "this_1",
      {
        dependsOn: [tfeSamlSettingsThis],
      }
    );
    /*This allows the Terraform resource name to match the original name. You can remove the call if you don't need them to match.*/
    tfeScimSettingsThis.overrideLogicalId("this");
  }
}

```

```typescript
import * as constructs from "constructs";
import * as cdktf from "cdktf";
/*Provider bindings are generated by running cdktf get.
See https://cdk.tf/provider-generation for more details.*/
import * as tfe from "./.gen/providers/tfe";
class MyConvertedCode extends cdktf.TerraformStack {
  constructor(scope: constructs.Construct, name: string) {
    super(scope, name);
    const tfeSamlSettingsThis = new tfe.samlSettings.SamlSettings(
      this,
      "this",
      {
        idpCert: "foobarCertificate",
        providerType: "okta",
        sloEndpointUrl: "https://example.com/slo_endpoint_url",
        ssoEndpointUrl: "https://example.com/sso_endpoint_url",
      }
    );
    const tfeScimSettingsThis = new tfe.scimSettings.ScimSettings(
      this,
      "this_1",
      {
        dependsOn: [tfeSamlSettingsThis],
      }
    );
    /*This allows the Terraform resource name to match the original name. You can remove the call if you don't need them to match.*/
    tfeScimSettingsThis.overrideLogicalId("this");
  }
}

```

```typescript
import * as constructs from "constructs";
import * as cdktf from "cdktf";
/*Provider bindings are generated by running cdktf get.
See https://cdk.tf/provider-generation for more details.*/
import * as tfe from "./.gen/providers/tfe";
class MyConvertedCode extends cdktf.TerraformStack {
  constructor(scope: constructs.Construct, name: string) {
    super(scope, name);
    /*Terraform Variables are not always the best fit for getting inputs in the context of Terraform CDK.
    You can read more about this at https://cdk.tf/variables*/
    const siteAdminGroupScimId = new cdktf.TerraformVariable(
      this,
      "site_admin_group_scim_id",
      {
        description: "SCIM ID of the group that should map to site admin.",
        type: cdktf.VariableType.STRING,
      }
    );
    const tfeSamlSettingsThis = new tfe.samlSettings.SamlSettings(
      this,
      "this",
      {
        idpCert: "foobarCertificate",
        providerType: "okta",
        sloEndpointUrl: "https://example.com/slo_endpoint_url",
        ssoEndpointUrl: "https://example.com/sso_endpoint_url",
      }
    );
    const tfeScimSettingsThis = new tfe.scimSettings.ScimSettings(
      this,
      "this_2",
      {
        dependsOn: [tfeSamlSettingsThis],
        siteAdminGroupScimId: siteAdminGroupScimId.stringValue,
      }
    );
    /*This allows the Terraform resource name to match the original name. You can remove the call if you don't need them to match.*/
    tfeScimSettingsThis.overrideLogicalId("this");
  }
}

```

```typescript
import * as constructs from "constructs";
import * as cdktf from "cdktf";
/*Provider bindings are generated by running cdktf get.
See https://cdk.tf/provider-generation for more details.*/
import * as tfe from "./.gen/providers/tfe";
class MyConvertedCode extends cdktf.TerraformStack {
  constructor(scope: constructs.Construct, name: string) {
    super(scope, name);
    const tfeSamlSettingsThis = new tfe.samlSettings.SamlSettings(
      this,
      "this",
      {
        idpCert: "foobarCertificate",
        providerType: "okta",
        sloEndpointUrl: "https://example.com/slo_endpoint_url",
        ssoEndpointUrl: "https://example.com/sso_endpoint_url",
      }
    );
    const tfeScimSettingsThis = new tfe.scimSettings.ScimSettings(
      this,
      "this_1",
      {
        dependsOn: [tfeSamlSettingsThis],
        paused: true,
      }
    );
    /*This allows the Terraform resource name to match the original name. You can remove the call if you don't need them to match.*/
    tfeScimSettingsThis.overrideLogicalId("this");
  }
}

```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `paused` (Boolean) Whether SCIM provisioning is paused. Defaults to `false`.
- `siteAdminGroupScimId` (String) SCIM ID of the group whose members are granted site admin privileges. Defaults to `""` (unlinked).

### Read-Only

- `enabled` (Boolean) Whether SCIM provisioning is enabled. Always `true` while this resource exists; use `terraform destroy` to disable. If SCIM is disabled outside of Terraform, the next `terraform plan` will propose re-creating this resource.
- `id` (String) The ID of the SCIM settings. Always `scim`.
- `siteAdminGroupDisplayName` (String) Display name of the group whose members are granted site admin privileges. Empty when no group is linked.



## Import

Resource tfe_scim_settings can be imported in the following format: 

```shell
# via the fixed ID 'scim'
terraform import tfe_scim_settings.this scim
```

<!-- cache-key: cdktf-0.17.0-pre.15 input-d540a51c62b7e6032c38161fee6e0c9c398bd3c4ba6d2c31b1e1fb4cf223befa -->