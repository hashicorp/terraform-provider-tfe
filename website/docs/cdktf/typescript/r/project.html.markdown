---
layout: "tfe"
page_title: "Terraform Enterprise: Resource tfe_project"
description: |-
  Manages a project.
---


<!-- Please do not edit this file, it is generated. -->
# Resource: tfe_project

Manages a project.

## Example Usage

```typescript
import * as constructs from "constructs";
import * as cdktf from "cdktf";
/*Provider bindings are generated by running cdktf get.
See https://cdk.tf/provider-generation for more details.*/
import * as tfe from "./.gen/providers/tfe";
class MyConvertedCode extends cdktf.TerraformStack {
  constructor(scope: constructs.Construct, name: string) {
    super(scope, name);
    const tfeOrganizationTestOrganization = new tfe.organization.Organization(
      this,
      "test-organization",
      {
        email: "admin@company.com",
        name: "my-org-name",
      }
    );
    new tfe.project.Project(this, "test", {
      name: "projectname",
      organization: cdktf.Token.asString(tfeOrganizationTestOrganization.name),
    });
  }
}

```

```typescript
import * as constructs from "constructs";
import * as cdktf from "cdktf";
/*Provider bindings are generated by running cdktf get.
See https://cdk.tf/provider-generation for more details.*/
import * as tfe from "./.gen/providers/tfe";
class MyConvertedCode extends cdktf.TerraformStack {
  constructor(scope: constructs.Construct, name: string) {
    super(scope, name);
    const tfeOrganizationTestOrganization = new tfe.organization.Organization(
      this,
      "test-organization",
      {
        email: "admin@company.com",
        name: "my-org-name",
      }
    );
    new tfe.project.Project(this, "test", {
      name: "projectname",
      organization: cdktf.Token.asString(tfeOrganizationTestOrganization.name),
      tags: {
        cost_center: "infrastructure",
        team: "platform",
      },
    });
  }
}

```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the project. TFE versions v202404-2 and earlier support between 3-36 characters. TFE versions v202405-1 and later support between 3-40 characters.

### Optional

- `autoDestroyActivityDuration` (String) A duration string for all workspaces in the project, representing time after each workspace's activity when an auto-destroy run will be triggered.
- `description` (String) Description of the project.
- `ignoreAdditionalTags` (Boolean) Explicitly ignores `tags` not defined by config so they will not be overwritten by the configured tags. This creates exceptional behaviour in Terraform with respect to `tags` and is not recommended. This value must be applied before it will be used.
- `organization` (String) Name of the organization. If omitted, organization must be defined in the provider config.
- `tags` (Map of String) A map of key-value tags to add to the project.

### Read-Only

- `id` (String) ID for the project.



## Import

tfe_project can be imported using an identity. For example:

```terraform
import {
  to = tfe_project.test
  identity = {
    id       = "prj-12345678"
    hostname = "app.terraform.io"
  }
}
```

<!-- schema generated by tfplugindocs -->
### Identity Schema

#### Required

- `id` (String)

#### Optional

- `hostname` (String)


Resource tfe_project can be imported in the following format: 

```shell
# via <PROJECT ID>
terraform import tfe_project.test prj-niVoeESBXT8ZREhr
```

<!-- cache-key: cdktf-0.17.0-pre.15 input-47c448a1146bcde0386539ee35ba654786f0c900d3af7a154db792d2042c11c0 -->