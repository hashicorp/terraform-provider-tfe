---
layout: "tfe"
page_title: "Terraform Enterprise: Resource tfe_stack_variable_set"
description: |-
  Manages associations between variable sets and stacks.
---


<!-- Please do not edit this file, it is generated. -->
# Resource: tfe_stack_variable_set

Manages associations between variable sets and stacks.

## Example Usage

```typescript
import * as constructs from "constructs";
import * as cdktf from "cdktf";
/*Provider bindings are generated by running cdktf get.
See https://cdk.tf/provider-generation for more details.*/
import * as tfe from "./.gen/providers/tfe";
class MyConvertedCode extends cdktf.TerraformStack {
  constructor(scope: constructs.Construct, name: string) {
    super(scope, name);
    const tfeOrganizationTest = new tfe.organization.Organization(
      this,
      "test",
      {
        email: "admin@company.com",
        name: "my-org-name",
      }
    );
    const tfeStackTest = new tfe.stack.Stack(this, "test_1", {
      name: "my-stack-name",
      projectId: cdktf.Token.asString(tfeOrganizationTest.defaultProjectId),
    });
    /*This allows the Terraform resource name to match the original name. You can remove the call if you don't need them to match.*/
    tfeStackTest.overrideLogicalId("test");
    const tfeVariableSetTest = new tfe.variableSet.VariableSet(this, "test_2", {
      description: "Some description.",
      name: "Test Varset",
      organization: cdktf.Token.asString(tfeOrganizationTest.id),
    });
    /*This allows the Terraform resource name to match the original name. You can remove the call if you don't need them to match.*/
    tfeVariableSetTest.overrideLogicalId("test");
    const tfeStackVariableSetTest = new tfe.stackVariableSet.StackVariableSet(
      this,
      "test_3",
      {
        stackId: cdktf.Token.asString(tfeStackTest.id),
        variableSetId: cdktf.Token.asString(tfeVariableSetTest.id),
      }
    );
    /*This allows the Terraform resource name to match the original name. You can remove the call if you don't need them to match.*/
    tfeStackVariableSetTest.overrideLogicalId("test");
  }
}

```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `stackId` (String) The ID of the stack to which to add the variable set.
- `variableSetId` (String) The ID of the variable set to add to the stack.

### Read-Only

- `id` (String) The ID of the stack variable set association in the format `stack/varset`.



## Import

Resource tfe_stack_variable_set can be imported in the following format: 

```shell
# via <STACK ID>/<VARSET ID>
terraform import tfe_stack_variable_set.test st-abcdefgh/varset-ijklmnop
```

<!-- cache-key: cdktf-0.17.0-pre.15 input-7cfe839d48672ef50aea20b39e2cbb40c07c8bdabde5b9eef2471befd8a680b9 -->