---
layout: "tfe"
page_title: "Terraform Enterprise: Data Source tfe_org_max_token_ttl_policy"
description: |-
  Retrieves the maximum time-to-live (TTL) policy for API tokens in an organization. This data source fetches the current TTL limits for organization, team, audit trail, and user tokens.
  ~> Note: This data source requires using the provider with HCP Terraform or an instance of Terraform Enterprise at least as recent as v2.0.1.
  ~> Warning: Maximum TTL policies are enforced immediately. Any existing tokens that exceed configured limits will stop working and return an "Unauthorized" error.
  Each token type exposes two attributes: a human-readable duration string (e.g. org_token_max_ttl) and a millisecond integer (e.g. org_token_max_ttl_ms). Use the duration string for display; use milliseconds for time arithmetic or comparisons.
  If no TTL policy has been configured for a token type, the data source returns a default value of 2 years for that type.
  To check whether the max TTL feature is enabled for an organization, use the max_ttl_enabled attribute on the tfe_organization resource or data source.
---


<!-- Please do not edit this file, it is generated. -->
# Data Source: tfe_org_max_token_ttl_policy

Retrieves the maximum time-to-live (TTL) policy for API tokens in an organization. This data source fetches the current TTL limits for organization, team, audit trail, and user tokens.

~> **Note:** This data source requires using the provider with HCP Terraform or an instance of Terraform Enterprise at least as recent as v2.0.1.

~> **Warning:** Maximum TTL policies are enforced immediately. Any existing tokens that exceed configured limits will stop working and return an "Unauthorized" error.

Each token type exposes two attributes: a human-readable duration string (e.g. `orgTokenMaxTtl`) and a millisecond integer (e.g. `orgTokenMaxTtlMs`). Use the duration string for display; use milliseconds for time arithmetic or comparisons.

If no TTL policy has been configured for a token type, the data source returns a default value of 2 years for that type.

To check whether the max TTL feature is enabled for an organization, use the `maxTtlEnabled` attribute on the `tfeOrganization` resource or data source.

## Example Usage

```typescript
import * as constructs from "constructs";
import * as cdktf from "cdktf";
/*Provider bindings are generated by running cdktf get.
See https://cdk.tf/provider-generation for more details.*/
import * as tfe from "./.gen/providers/tfe";
class MyConvertedCode extends cdktf.TerraformStack {
  constructor(scope: constructs.Construct, name: string) {
    super(scope, name);
    const dataTfeOrgMaxTokenTtlPolicyExample =
      new tfe.dataTfeOrgMaxTokenTtlPolicy.DataTfeOrgMaxTokenTtlPolicy(
        this,
        "example",
        {
          organization: "my-org-name",
        }
      );
    new cdktf.TerraformOutput(this, "org_token_ttl_ms", {
      value: dataTfeOrgMaxTokenTtlPolicyExample.orgTokenMaxTtlMs,
    });
  }
}

```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `organization` (String) Name of the organization. If omitted, organization must be defined in the provider config.

### Read-Only

- `auditTrailTokenMaxTtl` (String) Maximum lifespan allowed for audit trail tokens in human-readable duration format (e.g., `30D`, `6Mo`, `2Y`).
- `auditTrailTokenMaxTtlMs` (Number) Maximum lifespan allowed for audit trail tokens in milliseconds.
- `orgTokenMaxTtl` (String) Maximum lifespan allowed for organization tokens in human-readable duration format (e.g., `30D`, `6Mo`, `2Y`).
- `orgTokenMaxTtlMs` (Number) Maximum lifespan allowed for organization tokens in milliseconds.
- `teamTokenMaxTtl` (String) Maximum lifespan allowed for team tokens in human-readable duration format (e.g., `30D`, `6Mo`, `2Y`).
- `teamTokenMaxTtlMs` (Number) Maximum lifespan allowed for team tokens in milliseconds.
- `userTokenMaxTtl` (String) Maximum lifespan allowed for user tokens in human-readable duration format (e.g., `30D`, `6Mo`, `2Y`).
- `userTokenMaxTtlMs` (Number) Maximum lifespan allowed for user tokens in milliseconds.



<!-- cache-key: cdktf-0.17.0-pre.15 input-16de947f69f78fce4f7d4b0395a23bb27f3c18b452bbe9902ef1eb978f8119ad -->