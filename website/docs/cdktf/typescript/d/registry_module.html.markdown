---
layout: "tfe"
page_title: "Terraform Enterprise: Data Source tfe_registry_module"
description: |-
  Gets information about a public or private registry module.
  -> Note: Since modules have a required naming convention https://developer.hashicorp.com/terraform/registry/modules/publish#requirements, you can get naming values from your module repository (terraform-<module_provider>-<name>).
---


<!-- Please do not edit this file, it is generated. -->
# Data Source: tfe_registry_module

Gets information about a public or private registry module.

-> **Note:** Since modules have a [required naming convention](https://developer.hashicorp.com/terraform/registry/modules/publish#requirements), you can get naming values from your module repository (`terraform-<moduleProvider>-<name>`).

## Example Usage

```typescript
import * as constructs from "constructs";
import * as cdktf from "cdktf";
/*Provider bindings are generated by running cdktf get.
See https://cdk.tf/provider-generation for more details.*/
import * as tfe from "./.gen/providers/tfe";
class MyConvertedCode extends cdktf.TerraformStack {
  constructor(scope: constructs.Construct, name: string) {
    super(scope, name);
    new tfe.dataTfeRegistryModule.DataTfeRegistryModule(this, "example", {
      moduleProvider: "aws",
      name: "no-code-ssm",
      organization: "my-organization",
    });
  }
}

```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `moduleProvider` (String) Name of the module provider. Follows repository name convention `terraform-<provider>-<name>`.
- `name` (String) Name of the module. Follows repository name convention `terraform-<provider>-<name>`.
- `organization` (String) Name of the organization associated with the registry module.

### Optional

- `namespace` (String) The namespace of the registry module. Defaults to the organization name, the `organization` value, if not provided.
- `registryName` (String) Name of the registry. Valid options: "public", "private". Defaults to "private".

### Read-Only

- `createdAt` (String) The time when the module was created.
- `id` (String) ID of the registry module.
- `noCode` (Boolean) Whether or not this is a no-code module.
- `noCodeModuleId` (String) ID of the no-code module.
- `noCodeModuleSource` (String) Source value of the no-code module (`<ORGANIZATION>/<REGISTRY_NAME>/<NAMESPACE>/<REGISTRY MODULE NAME>/<REGISTRY MODULE PROVIDER>`).
- `permissions` (Block List) The permissions for this module. (see [below for nested schema](#nestedblock--permissions))
- `publishingMechanism` (String) The publishing mechanism of the module used when releasing new versions of the module.
- `status` (String) The current status of the module.
- `testConfig` (Block List) Test configuration indicating module testing setup. (see [below for nested schema](#nestedblock--test_config))
- `updatedAt` (String) The time when the module was last updated.
- `vcsRepo` (Block List) Settings for the registry module's VCS repository. (see [below for nested schema](#nestedblock--vcs_repo))
- `versionStatuses` (Block List) The status of each version of this module. (see [below for nested schema](#nestedblock--version_statuses))

<a id="nestedblock--permissions"></a>
### Nested Schema for `permissions`

Read-Only:

- `canDelete` (Boolean) Whether or not this is a delete permission.
- `canResync` (Boolean) Whether or not this is a resync permission.
- `canRetry` (Boolean) Whether or not this is a retry permission.


<a id="nestedblock--test_config"></a>
### Nested Schema for `testConfig`

Read-Only:

- `testsEnabled` (Boolean) Indicates whether tests are enabled for a module.


<a id="nestedblock--vcs_repo"></a>
### Nested Schema for `vcsRepo`

Read-Only:

- `branch` (String) The git branch used for publishing when using branch-based publishing for the registry module. When a `branch` is set, `tags` will be returned as `false`.
- `displayIdentifier` (String) The display identifier for your VCS repository. For most VCS providers outside of BitBucket Cloud and Azure DevOps, this will match the `identifier` string.
- `githubAppInstallationId` (String) The installation id of the Github App. This conflicts with `oauthTokenId` and can only be used if `oauthTokenId` is not used.
- `identifier` (String) A reference to your VCS repository in the format `<organization>/<repository>` where `<organization>` and `<repository>` refer to the organization (or project key, for Bitbucket Data Center) and repository in your VCS provider. The format for Azure DevOps is `<ado organization>/<ado project>/_git/<ado repository>`.
- `ingressSubmodules` (Boolean) Indicates whether submodules should be fetched when cloning the VCS repository.
- `oauthTokenId` (String) Token ID of the VCS Connection (OAuth Connection Token) to use. This conflicts with `githubAppInstallationId` and can only be used if `githubAppInstallationId` is not used.
- `repositoryHttpUrl` (String) The browsable HTTPS URL of the VCS repository.
- `serviceProvider` (String) The VCS service provider type. Valid values include `github`, `githubEnterprise`, `gitlabHosted`, `gitlabCommunityEdition`, `gitlabEnterpriseEdition`, `bitbucketHosted`, `bitbucketDataCenter`, `bitbucketServer`, `adoServer`, `adoServices`.
- `sourceDirectory` (String) The path to the module configuration files within the VCS repository. This feature is currently in beta and is not available to all users.
- `tagPrefix` (String) The prefix to filter repository Git tags when using the tag-based publishing type in a repository that contains code for multiple modules. Without a prefix, HCP Terraform and Terraform Enterprise publish new versions for all modules with valid Git tags that use semantic versioning. This feature is currently in beta and is not available to all users.
- `tags` (Boolean) Specifies whether tag based publishing is enabled for the registry module. When `tags` is set to `true`, the `branch` must be set to an empty value.
- `tagsRegex` (String) Read-only echo of any tags regex stored on the VCS repository.
- `webhookUrl` (String) The inbound webhook URL registered with the VCS provider. This is the HCP Terraform or Terraform Enterprise endpoint that the VCS provider calls on push or pull-request events.


<a id="nestedblock--version_statuses"></a>
### Nested Schema for `versionStatuses`

Read-Only:

- `error` (String) Error message reported by module at specific version.
- `status` (String) Status of the module at specific version.
- `version` (String) The version of this module.




<!-- cache-key: cdktf-0.17.0-pre.15 input-1aa6836ff55d007deb6334af7a937dc0842a438a62d2d192d8845dd441360794 -->