---
layout: "tfe"
page_title: "Terraform Enterprise: Data Source tfe_scim_token"
description: |-
  (Only for Terraform Enterprise) Gets information on a SCIM authentication token. The token value itself is never returned by the real endpoint; only metadata about the token is available.
  Requires SCIM to be enabled. Requires admin token configuration. See example usage for incorporating an admin token in your provider config.
---


<!-- Please do not edit this file, it is generated. -->
# Data Source: tfe_scim_token

(Only for Terraform Enterprise) Gets information on a SCIM authentication token. The token value itself is never returned by the real endpoint; only metadata about the token is available.

Requires SCIM to be enabled. Requires admin token configuration. See example usage for incorporating an admin token in your provider config.

## Example Usage

```python
import constructs as constructs
import cdktf as cdktf
# Provider bindings are generated by running cdktf get.
# See https://cdk.tf/provider-generation for more details.
import ...gen.providers.tfe as tfe
class MyConvertedCode(cdktf.TerraformStack):
    def __init__(self, scope, name):
        super().__init__(scope, name)
        # Terraform Variables are not always the best fit for getting inputs in the context of Terraform CDK.
        #     You can read more about this at https://cdk.tf/variables
        admin_token = cdktf.TerraformVariable(self, "admin_token",
            description="An admin access token"
        )
        hostname = cdktf.TerraformVariable(self, "hostname",
            default="app.terraform.io",
            description="The HCP Terraform or Enterprise hostname."
        )
        token = cdktf.TerraformVariable(self, "token",
            description="An access token"
        )
        tfe.provider.TfeProvider(self, "tfe",
            hostname=hostname.string_value,
            token=token.string_value
        )
        tfe_admin = tfe.provider.TfeProvider(self, "tfe_4",
            alias="admin",
            hostname=hostname.string_value,
            token=admin_token.string_value
        )
        tfe.data_tfe_scim_token.DataTfeScimToken(self, "foo",
            id="at-XXXXXXXXXXXXXXXX",
            provider=tfe_admin
        )
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (String) The ID of the SCIM token. Starts with `at-`.

### Read-Only

- `created_at` (String) The time when the SCIM token was created.
- `description` (String) The description of the SCIM token.
- `expired_at` (String) The time when the SCIM token expires.
- `last_used_at` (String) The time when the SCIM token was last used.



<!-- cache-key: cdktf-0.17.0-pre.15 input-7be3b5ea923471fa30bdc84fd918a89f8b591215fd6b83408b0931efd17ce47a -->