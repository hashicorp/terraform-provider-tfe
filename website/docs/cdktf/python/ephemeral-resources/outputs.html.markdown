---
layout: "tfe"
page_title: "Terraform Enterprise: Ephemeral tfe_outputs"
description: |-
  This ephemeral resource can be used to retrieve state outputs for a given workspace. It enables output values in one Terraform configuration to be used in another. The retrieved output values are guaranteed not to be written to state.
  ~> Warning: Ephemeral resources are a new feature and may evolve as we continue to explore their most effective uses. Learn more https://developer.hashicorp.com/terraform/language/v1.10.x/resources/ephemeral.
  ~> Note: Regardless of sensitivity of the output values as set in HCP Terraform, this ephemeral resource treats both values and nonsensitive_values as sensitive.
---


<!-- Please do not edit this file, it is generated. -->
# Ephemeral Resource: tfe_outputs

This ephemeral resource can be used to retrieve state outputs for a given workspace. It enables output values in one Terraform configuration to be used in another. The retrieved output values are guaranteed not to be written to state.

~> **Warning:** Ephemeral resources are a new feature and may evolve as we continue to explore their most effective uses. [Learn more](https://developer.hashicorp.com/terraform/language/v1.10.x/resources/ephemeral).

~> **Note:** Regardless of sensitivity of the output values as set in HCP Terraform, this ephemeral resource treats both `values` and `nonsensitive_values` as sensitive.

## Example Usage

```terraform
# Using the `tfe_outputs` ephemeral resource, the outputs `vault_role_id` and `vault_secret_id` can be used to configure a vault provider instance as seen below:
# In the example below, assume we have outputs defined in a my-org/my-workspace

ephemeral "tfe_outputs" "foo" {
  organization = "my-org"
  workspace    = "my-workspace"
}

provider "vault" {
  auth_login {
    path = "auth/approle/login"

    parameters = {
      role_id   = ephemeral_tfe_outputs.foo.values.vault_role_id
      secret_id = ephemeral_tfe_outputs.foo.values.vault_secret_id
    }
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `workspace` (String) Name of the workspace.

### Optional

- `organization` (String) Name of the organization. If omitted, the organization must be defined in the provider config.

### Read-Only

- `id` (String) System-generated unique identifier for the resource. Do not rely on this value.
- `nonsensitive_values` (Dynamic) The current non-sensitive output values for the specified workspace, this is a subset of all output values.
- `values` (Dynamic) The current output values for the specified workspace.



<!-- cache-key: cdktf-0.17.0-pre.15 input-b4192eb0821cf77cc43eeec619ef00425e3d0744ca18dcdfadb99072ae412677 -->