---
layout: "tfe"
page_title: "Terraform Enterprise: Resource tfe_test_variable"
description: |-
  Creates, updates and destroys environment variables used for testing in the Private Module Registry.
---


<!-- Please do not edit this file, it is generated. -->
# Resource: tfe_test_variable

Creates, updates and destroys environment variables used for testing in the Private Module Registry.

## Example Usage

```python
import constructs as constructs
import cdktf as cdktf
# Provider bindings are generated by running cdktf get.
# See https://cdk.tf/provider-generation for more details.
import ...gen.providers.tfe as tfe
class MyConvertedCode(cdktf.TerraformStack):
    def __init__(self, scope, name):
        super().__init__(scope, name)
        tfe_organization_test_org = tfe.organization.Organization(self, "test_org",
            email="admin@company.com",
            name="my-org-name"
        )
        tfe_oauth_client_test_client = tfe.oauth_client.OauthClient(self, "test_client",
            api_url="https://api.github.com",
            http_url="https://github.com",
            oauth_token="my-token-123",
            organization=cdktf.Token.as_string(tfe_organization_test_org.name),
            service_provider="github"
        )
        tfe_registry_module_test_module = tfe.registry_module.RegistryModule(self, "test_module",
            organization="test-module",
            test_config=[RegistryModuleTestConfig(
                tests_enabled=True
            )
            ],
            vcs_repo=RegistryModuleVcsRepo(
                branch="main",
                display_identifier="GH_NAME/REPO_NAME",
                identifier="GH_NAME/REPO_NAME",
                oauth_token_id=cdktf.Token.as_string(tfe_oauth_client_test_client.oauth_token_id),
                tags=False
            )
        )
        tfe.test_variable.TestVariable(self, "tf_test_test_variable",
            category="env",
            description="some description",
            key="key_test",
            module_name=cdktf.Token.as_string(tfe_registry_module_test_module.name),
            module_provider=cdktf.Token.as_string(tfe_registry_module_test_module.module_provider),
            organization=cdktf.Token.as_string(tfe_organization_test_org.name),
            value="value_test"
        )
```

```terraform
# Usage of the write-only value for tfe_test_variable

variable "session_token" {
  description = "The session token for use in the write only field"
}

resource "tfe_organization" "test_org" {
  name  = "my-org-name"
  email = "admin@company.com"
}

resource "tfe_oauth_client" "test_client" {
  organization     = tfe_organization.test_org.name
  api_url          = "https://api.github.com"
  http_url         = "https://github.com"
  oauth_token      = "my-token-123"
  service_provider = "github"
}

resource "tfe_registry_module" "test_module" {
  organization = "test-module"
  vcs_repo {
    display_identifier = "GH_NAME/REPO_NAME"
    identifier         = "GH_NAME/REPO_NAME"
    oauth_token_id     = tfe_oauth_client.test_client.oauth_token_id
    branch             = "main"
    tags               = false
  }
  test_config {
    tests_enabled = true
  }
}

resource "tfe_test_variable" "tf_test_test_variable" {
  key              = "key_test"
  value_wo         = var.session_token
  value_wo_version = 1
  description      = "some description"
  category         = "env"
  organization     = tfe_organization.test_org.name
  module_name      = tfe_registry_module.test_module.name
  module_provider  = tfe_registry_module.test_module.module_provider
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `category` (String) Whether this is a Terraform or environment variable. Valid values are `"terraform"` or `"env"`.
- `key` (String) Name of the variable.
- `module_name` (String) The name of the registry module on which this test variable is set.
- `module_provider` (String) The provider of the registry module on which this test variable is set.
- `organization` (String) Name of the organization that owns the registry module on which this test variable is set.

### Optional

> **NOTE**: [Write-only arguments](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments) are supported in Terraform 1.11 and later.

- `description` (String) Description of the variable. Defaults to `""`
- `hcl` (Boolean) Whether to evaluate the value of the variable as a string of HCL code. Has no effect for environment variables. Defaults to `false`.
- `sensitive` (Boolean) Whether the value is sensitive. If true then the variable is written once and not visible thereafter. Defaults to `false`.
- `value` (String, Sensitive) Value of the variable. Defaults to `""`. Cannot be used with `value_wo`.
- `value_wo` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Value of the variable in write-only mode. Cannot be used with `value`.
- `value_wo_version` (Number) Version identifier for the write-only value. Required when `value_wo` is specified to trigger updates. Cannot be used with `value`.

### Read-Only

- `id` (String) The ID of the variable.
- `readable_value` (String) A non-sensitive read-only copy of the variable value, which can be viewed or referenced in plan outputs without being redacted. Will only be present if the variable is not sensitive.



<!-- cache-key: cdktf-0.17.0-pre.15 input-d82a101cbfa6129b5c1e13edd8757f3ac92caf3836c322d8c4a6e07a2c7e4ba7 -->