---
layout: "tfe"
page_title: "Terraform Enterprise: Resource tfe_scim_group_mapping"
description: |-
  Maps a SCIM group to a team in Terraform Enterprise.
  When a team is mapped to a SCIM group, its membership is managed by your Identity Provider (IdP) through SCIM provisioning. A team can be mapped to at most one SCIM group.
  This resource applies only to Terraform Enterprise and requires admin token configuration. SCIM (and SAML) must be enabled first; see tfe_scim_settings scim_settings.html for details. The SCIM group you map to must already be provisioned into Terraform Enterprise by your IdP before the mapping can be created.
  ~> Note: Creating a mapping with paused = true does not skip the initial sync. The mapping API does not support creating a mapping in a paused state, so the mapping is created active and then paused in a follow-up request. This means an initial sync of the SCIM group's members onto the team runs before the pause takes effect, and any changes made to the SCIM group during that brief window may be reflected on the team
---


<!-- Please do not edit this file, it is generated. -->
# Resource: tfe_scim_group_mapping

Maps a SCIM group to a team in Terraform Enterprise.

When a team is mapped to a SCIM group, its membership is managed by your Identity Provider (IdP) through SCIM provisioning. A team can be mapped to at most one SCIM group.

This resource applies only to Terraform Enterprise and requires admin token configuration. SCIM (and SAML) must be enabled first; see [`tfe_scim_settings`](scim_settings.html) for details. The SCIM group you map to must already be provisioned into Terraform Enterprise by your IdP before the mapping can be created.

~> **Note:** Creating a mapping with `paused = true` does not skip the initial sync. The mapping API does not support creating a mapping in a paused state, so the mapping is created active and then paused in a follow-up request. This means an initial sync of the SCIM group's members onto the team runs before the pause takes effect, and any changes made to the SCIM group during that brief window may be reflected on the team

## Example Usage

```python
import constructs as constructs
import cdktf as cdktf
# Provider bindings are generated by running cdktf get.
# See https://cdk.tf/provider-generation for more details.
import ...gen.providers.tfe as tfe
class MyConvertedCode(cdktf.TerraformStack):
    def __init__(self, scope, name):
        super().__init__(scope, name)
        # Terraform Variables are not always the best fit for getting inputs in the context of Terraform CDK.
        #     You can read more about this at https://cdk.tf/variables
        admin_token = cdktf.TerraformVariable(self, "admin_token",
            description="An admin access token"
        )
        hostname = cdktf.TerraformVariable(self, "hostname",
            default="app.terraform.io",
            description="The HCP Terraform or Enterprise hostname."
        )
        tfe_saml_settings_this = tfe.saml_settings.SamlSettings(self, "this",
            idp_cert="foobarCertificate",
            provider_type="okta",
            slo_endpoint_url="https://example.com/slo_endpoint_url",
            sso_endpoint_url="https://example.com/sso_endpoint_url"
        )
        tfe_scim_settings_this = tfe.scim_settings.ScimSettings(self, "this_3",
            depends_on=[tfe_saml_settings_this]
        )
        # This allows the Terraform resource name to match the original name. You can remove the call if you don't need them to match.
        tfe_scim_settings_this.override_logical_id("this")
        data_tfe_organization_this =
        tfe.data_tfe_organization.DataTfeOrganization(self, "this_4",
            name="my-org-name"
        )
        # This allows the Terraform resource name to match the original name. You can remove the call if you don't need them to match.
        data_tfe_organization_this.override_logical_id("this")
        data_tfe_scim_group_engineering =
        tfe.data_tfe_scim_group.DataTfeScimGroup(self, "engineering",
            depends_on=[tfe_scim_settings_this],
            name="engineering-scim-group"
        )
        tfe.provider.TfeProvider(self, "tfe",
            hostname=hostname.string_value,
            token=admin_token.string_value
        )
        tfe_team_engineering = tfe.team.Team(self, "engineering_7",
            name="engineering",
            organization=cdktf.Token.as_string(data_tfe_organization_this.name)
        )
        # This allows the Terraform resource name to match the original name. You can remove the call if you don't need them to match.
        tfe_team_engineering.override_logical_id("engineering")
        tfe_scim_group_mapping_engineering =
        tfe.scim_group_mapping.ScimGroupMapping(self, "engineering_8",
            scim_group_id=cdktf.Token.as_string(data_tfe_scim_group_engineering.id),
            team_id=cdktf.Token.as_string(tfe_team_engineering.id)
        )
        # This allows the Terraform resource name to match the original name. You can remove the call if you don't need them to match.
        tfe_scim_group_mapping_engineering.override_logical_id("engineering")
```

```python
import constructs as constructs
import cdktf as cdktf
# Provider bindings are generated by running cdktf get.
# See https://cdk.tf/provider-generation for more details.
import ...gen.providers.tfe as tfe
class MyConvertedCode(cdktf.TerraformStack):
    def __init__(self, scope, name):
        super().__init__(scope, name)
        tfe_saml_settings_this = tfe.saml_settings.SamlSettings(self, "this",
            idp_cert="foobarCertificate",
            provider_type="okta",
            slo_endpoint_url="https://example.com/slo_endpoint_url",
            sso_endpoint_url="https://example.com/sso_endpoint_url"
        )
        tfe_scim_settings_this = tfe.scim_settings.ScimSettings(self, "this_1",
            depends_on=[tfe_saml_settings_this]
        )
        # This allows the Terraform resource name to match the original name. You can remove the call if you don't need them to match.
        tfe_scim_settings_this.override_logical_id("this")
        data_tfe_organization_this =
        tfe.data_tfe_organization.DataTfeOrganization(self, "this_2",
            name="my-org-name"
        )
        # This allows the Terraform resource name to match the original name. You can remove the call if you don't need them to match.
        data_tfe_organization_this.override_logical_id("this")
        data_tfe_scim_group_engineering =
        tfe.data_tfe_scim_group.DataTfeScimGroup(self, "engineering",
            depends_on=[tfe_scim_settings_this],
            name="engineering-scim-group"
        )
        tfe_team_engineering = tfe.team.Team(self, "engineering_4",
            name="engineering",
            organization=cdktf.Token.as_string(data_tfe_organization_this.name)
        )
        # This allows the Terraform resource name to match the original name. You can remove the call if you don't need them to match.
        tfe_team_engineering.override_logical_id("engineering")
        tfe_scim_group_mapping_engineering =
        tfe.scim_group_mapping.ScimGroupMapping(self, "engineering_5",
            paused=True,
            scim_group_id=cdktf.Token.as_string(data_tfe_scim_group_engineering.id),
            team_id=cdktf.Token.as_string(tfe_team_engineering.id)
        )
        # This allows the Terraform resource name to match the original name. You can remove the call if you don't need them to match.
        tfe_scim_group_mapping_engineering.override_logical_id("engineering")
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `scim_group_id` (String) The ID of the SCIM group to map to the team. Changing this forces a new mapping to be created, since the mapping API only supports updating the paused state.
- `team_id` (String) The ID of the team to map the SCIM group to. Changing this forces a new mapping to be created.

### Optional

- `paused` (Boolean) Whether provisioning for this mapping is paused. Defaults to `false`. Setting this to `true` on creation does not skip the initial sync, since the mapping is created active and then paused in a follow-up request.

### Read-Only

- `id` (String) The ID of the SCIM group mapping. Since a team can only be mapped to one SCIM group, this is the same as `team_id`.



## Import

Resource tfe_scim_group_mapping can be imported in the following format: 

```shell
# via <TEAM ID>
terraform import tfe_scim_group_mapping.engineering team-xxxxxxxxxxxxxxxx
```

<!-- cache-key: cdktf-0.17.0-pre.15 input-9f860dc83be9d3b5bf0ca068aba5023a1109818fe8cb093d056e86c3fbc4678f -->