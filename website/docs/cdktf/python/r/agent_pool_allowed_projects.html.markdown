---
layout: "tfe"
page_title: "Terraform Enterprise: tfe_agent_pool_allowed_projects"
description: |-
  Manages allowed projects on agent pools
---


<!-- Please do not edit this file, it is generated. -->
# tfe_agent_pool_allowed_projects

Adds and removes allowed projects on an agent pool.

~> **NOTE:** This resource requires using the provider with HCP Terraform and a HCP Terraform
for Business account.
[Learn more about HCP Terraform pricing here](https://www.hashicorp.com/products/terraform/pricing).

## Example Usage

```python
# DO NOT EDIT. Code generated by 'cdktf convert' - Please report bugs at https://cdk.tf/bug
from constructs import Construct
from cdktf import TerraformStack
#
# Provider bindings are generated by running `cdktf get`.
# See https://cdk.tf/provider-generation for more details.
#
from imports.tfe.agent_pool import AgentPool
from imports.tfe.agent_pool_allowed_projects import AgentPoolAllowedProjects
from imports.tfe.organization import Organization
from imports.tfe.project import Project
class MyConvertedCode(TerraformStack):
    def __init__(self, scope, name):
        super().__init__(scope, name)
        test_organization = Organization(self, "test-organization",
            email="admin@company.com",
            name="my-org-name"
        )
        test_project = Project(self, "test-project",
            name="my-project-name",
            organization=test_organization.name
        )
        test_agent_pool = AgentPool(self, "test-agent-pool",
            name="my-agent-pool-name",
            organization=test_organization.name,
            organization_scoped=False
        )
        AgentPoolAllowedProjects(self, "allowed",
            agent_pool_id=test_agent_pool.id,
            allowed_project_ids=[test_project.id]
        )
```

## Argument Reference

The following arguments are supported:

* `agent_pool_id` - (Required) The ID of the agent pool.
* `allowed_project_ids` - (Required) IDs of projects to be added as allowed projects on the agent pool.


## Import

A resource can be imported; use `<AGENT POOL ID>` as the import ID. For example:

```shell
terraform import tfe_agent_pool_allowed_projects.foobar apool-rW0KoLSlnuNb5adB
```

<!-- cache-key: cdktf-0.20.8 input-909a66e187e96fc31fdfab93b4e7a19876b13f346a7f1eab3aa4ab16f24eae5b -->