---
layout: "tfe"
page_title: "Terraform Enterprise: Resource tfe_agent_pool_allowed_projects"
description: |-
  Adds and removes allowed projects on an agent pool.
  ~> Note: This resource requires using the provider with HCP Terraform and a HCP Terraform for Business tier plan. Learn more about HCP Terraform pricing here https://www.hashicorp.com/products/terraform/pricing.
---


<!-- Please do not edit this file, it is generated. -->
# Resource: tfe_agent_pool_allowed_projects

Adds and removes allowed projects on an agent pool.

~> **Note:** This resource requires using the provider with HCP Terraform and a HCP Terraform for Business tier plan. [Learn more about HCP Terraform pricing here](https://www.hashicorp.com/products/terraform/pricing).

## Example Usage

```python
import constructs as constructs
import cdktf as cdktf
# Provider bindings are generated by running cdktf get.
# See https://cdk.tf/provider-generation for more details.
import ...gen.providers.tfe as tfe
class MyConvertedCode(cdktf.TerraformStack):
    def __init__(self, scope, name):
        super().__init__(scope, name)
        tfe_organization_test_organization = tfe.organization.Organization(self, "test-organization",
            email="admin@company.com",
            name="my-org-name"
        )
        tfe_project_test_project = tfe.project.Project(self, "test-project",
            name="my-project-name",
            organization=cdktf.Token.as_string(tfe_organization_test_organization.name)
        )
        tfe_agent_pool_test_agent_pool = tfe.agent_pool.AgentPool(self, "test-agent-pool",
            name="my-agent-pool-name",
            organization=cdktf.Token.as_string(tfe_organization_test_organization.name),
            organization_scoped=False
        )
        tfe.agent_pool_allowed_projects.AgentPoolAllowedProjects(self, "allowed",
            agent_pool_id=cdktf.Token.as_string(tfe_agent_pool_test_agent_pool.id),
            allowed_project_ids=[cdktf.Token.as_string(tfe_project_test_project.id)]
        )
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `agent_pool_id` (String) The ID of the agent pool.
- `allowed_project_ids` (Set of String) IDs of projects to be added as allowed projects on the agent pool.

### Read-Only

- `id` (String) The ID of this resource. Do not rely on this value — use `agent_pool_id` instead.



## Import

Resource tfe_agent_pool_allowed_projects can be imported in the following format: 

```shell
# via <AGENT POOL ID>
terraform import tfe_agent_pool_allowed_projects.foobar apool-rW0KoLSlnuNb5adB
```

<!-- cache-key: cdktf-0.17.0-pre.15 input-a4e1438f0d35df4b9c101353bc1c916146f3deb82064b5ee3d00ae082c886e77 -->