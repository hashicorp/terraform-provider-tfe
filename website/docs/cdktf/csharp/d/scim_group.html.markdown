---
layout: "tfe"
page_title: "Terraform Enterprise: Data Source tfe_scim_group"
description: |-
  (Only for Terraform Enterprise) Gets information on a SCIM group synchronized from an Identity Provider.
  Requires admin token configurations. See example usage for incorporating an admin token into your provider config. Use name to look up a single group by its exact display name (case-insensitive). The data source filters out fuzzy substring matches returned by the API and keeps only exact matches.
---


<!-- Please do not edit this file, it is generated. -->
# Data Source: tfe_scim_group

(Only for Terraform Enterprise) Gets information on a SCIM group synchronized from an Identity Provider.

Requires admin token configurations. See example usage for incorporating an admin token into your provider config. Use `Name` to look up a single group by its exact display name (case-insensitive). The data source filters out fuzzy substring matches returned by the API and keeps only exact matches.

## Example Usage

```csharp
using Constructs;
using HashiCorp.Cdktf;
/*Provider bindings are generated by running cdktf get.
See https://cdk.tf/provider-generation for more details.*/
using Gen.Providers.Tfe;
class MyConvertedCode : TerraformStack
{
    public MyConvertedCode(Construct scope, string name) : base(scope, name)
    {
        /*Terraform Variables are not always the best fit for getting inputs in the context of Terraform CDK.
            You can read more about this at https://cdk.tf/variables*/
        var adminToken = new TerraformVariable(this, "admin_token", new TerraformVariableConfig {
            Description = "An admin access token"
        });
        var hostname = new TerraformVariable(this, "hostname", new TerraformVariableConfig {
            Default = "app.terraform.io",
            Description = "The HCP Terraform or Enterprise hostname."
        });
        var token = new TerraformVariable(this, "token", new TerraformVariableConfig {
            Description = "An access token"
        });
        new Provider.TfeProvider(this, "tfe", new TfeProviderConfig {
            Hostname = hostname.StringValue,
            Token = token.StringValue
        });
        var tfeAdmin = new Provider.TfeProvider(this, "tfe_4", new TfeProviderConfig {
            Alias = "admin",
            Hostname = hostname.StringValue,
            Token = adminToken.StringValue
        });
        var dataTfeScimGroupAdmins = new DataTfeScimGroup.DataTfeScimGroup(this, "admins", new DataTfeScimGroupConfig {
            Name = "platform-admins",
            Provider = tfeAdmin
        });
        new TerraformOutput(this, "admin_group_id", new TerraformOutputConfig {
            Value = dataTfeScimGroupAdmins.Id
        });
    }
}
```

```csharp
using Constructs;
using HashiCorp.Cdktf;
/*Provider bindings are generated by running cdktf get.
See https://cdk.tf/provider-generation for more details.*/
using Gen.Providers.Tfe;
class MyConvertedCode : TerraformStack
{
    public MyConvertedCode(Construct scope, string name) : base(scope, name)
    {
        /*Terraform Variables are not always the best fit for getting inputs in the context of Terraform CDK.
            You can read more about this at https://cdk.tf/variables*/
        var adminToken = new TerraformVariable(this, "admin_token", new TerraformVariableConfig {
            Description = "An admin access token"
        });
        var hostname = new TerraformVariable(this, "hostname", new TerraformVariableConfig {
            Default = "app.terraform.io",
            Description = "The HCP Terraform or Enterprise hostname."
        });
        var token = new TerraformVariable(this, "token", new TerraformVariableConfig {
            Description = "An access token"
        });
        new Provider.TfeProvider(this, "tfe", new TfeProviderConfig {
            Hostname = hostname.StringValue,
            Token = token.StringValue
        });
        var tfeAdmin = new Provider.TfeProvider(this, "tfe_4", new TfeProviderConfig {
            Alias = "admin",
            Hostname = hostname.StringValue,
            Token = adminToken.StringValue
        });
        var dataTfeScimGroupSiteAdmins =
        new DataTfeScimGroup.DataTfeScimGroup(this, "site_admins", new DataTfeScimGroupConfig {
            Name = "tfe-site-admins",
            Provider = tfeAdmin
        });
        new ScimSettings.ScimSettings(this, "this", new ScimSettingsConfig {
            Provider = tfeAdmin,
            SiteAdminGroupScimId = Token.AsString(dataTfeScimGroupSiteAdmins.Id)
        });
    }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `Name` (String) The exact name of the SCIM group to retrieve (case-insensitive).

### Read-Only

- `Id` (String) The ID of the SCIM group.



<!-- cache-key: cdktf-0.17.0-pre.15 input-ecb319a2f0ec276bfec3632e5472219b70f84d6b200dd73d27dd84ab452b9f73 -->