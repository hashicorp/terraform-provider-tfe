---
layout: "tfe"
page_title: "Terraform Enterprise: Resource tfe_agent_pool_allowed_workspaces"
description: |-
  Adds and removes allowed workspaces on an agent pool.
  ~> Note: This resource requires using the provider with HCP Terraform and a HCP Terraform for Business account. Learn more about HCP Terraform pricing here https://www.hashicorp.com/products/terraform/pricing.
---


<!-- Please do not edit this file, it is generated. -->
# Resource: tfe_agent_pool_allowed_workspaces

Adds and removes allowed workspaces on an agent pool.

~> **Note:** This resource requires using the provider with HCP Terraform and a HCP Terraform for Business account. [Learn more about HCP Terraform pricing here](https://www.hashicorp.com/products/terraform/pricing).

## Example Usage

```csharp
using Constructs;
using HashiCorp.Cdktf;
/*Provider bindings are generated by running cdktf get.
See https://cdk.tf/provider-generation for more details.*/
using Gen.Providers.Tfe;
class MyConvertedCode : TerraformStack
{
    public MyConvertedCode(Construct scope, string name) : base(scope, name)
    {
        var tfeOrganizationTestOrganization = new Organization.Organization(this, "test-organization", new OrganizationConfig {
            Email = "admin@company.com",
            Name = "my-org-name"
        });
        var tfeWorkspaceTestWorkspace = new Workspace.Workspace(this, "test-workspace", new WorkspaceConfig {
            Name = "my-workspace-name",
            Organization = Token.AsString(tfeOrganizationTestOrganization.Name)
        });
        var tfeAgentPoolTestAgentPool = new AgentPool.AgentPool(this, "test-agent-pool", new AgentPoolConfig {
            Name = "my-agent-pool-name",
            Organization = Token.AsString(tfeOrganizationTestOrganization.Name),
            OrganizationScoped = false
        });
        var tfeAgentPoolAllowedWorkspacesAllowed =
        new AgentPoolAllowedWorkspaces.AgentPoolAllowedWorkspaces(this, "allowed", new AgentPoolAllowedWorkspacesConfig {
            AgentPoolId = Token.AsString(tfeAgentPoolTestAgentPool.Id),
            AllowedWorkspaceIds = new [] { Token.AsString(tfeWorkspaceTestWorkspace.Id) }
        });
        new WorkspaceSettings.WorkspaceSettings(this, "test-workspace-settings", new WorkspaceSettingsConfig {
            AgentPoolId = Token.AsString(tfeAgentPoolAllowedWorkspacesAllowed.Id),
            ExecutionMode = "agent",
            WorkspaceId = Token.AsString(tfeWorkspaceTestWorkspace.Id)
        });
    }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `AgentPoolId` (String) The ID of the agent pool.
- `AllowedWorkspaceIds` (Set of String) IDs of workspaces to be added as allowed workspaces on the agent pool.

### Read-Only

- `Id` (String) The ID of this resource. Do not rely on this value — use `AgentPoolId` instead.



## Import

Resource tfe_agent_pool_allowed_workspaces can be imported in the following format: 

```shell
# via <AGENT POOL ID>
terraform import tfe_agent_pool_allowed_workspaces.foobar apool-rW0KoLSlnuNb5adB
```

<!-- cache-key: cdktf-0.17.0-pre.15 input-0772e8a679b075ad7a13a5f63cde22035432ebd92797b013731856baa5561f67 -->