---
layout: "tfe"
page_title: "Terraform Enterprise: Resource tfe_organization_run_task"
description: |-
  Creates, updates and destroys Organization Run tasks https://developer.hashicorp.com/terraform/cloud-docs/workspaces/settings/run-tasks#creating-a-run-task.
  Run tasks https://developer.hashicorp.com/terraform/cloud-docs/workspaces/settings/run-tasks allow HCP Terraform to interact with external systems at specific points in the HCP Terraform run lifecycle. Run tasks are reusable configurations that you can attach to any workspace in an organization.
---


<!-- Please do not edit this file, it is generated. -->
# Resource: tfe_organization_run_task

Creates, updates and destroys [Organization Run tasks](https://developer.hashicorp.com/terraform/cloud-docs/workspaces/settings/run-tasks#creating-a-run-task).

[Run tasks](https://developer.hashicorp.com/terraform/cloud-docs/workspaces/settings/run-tasks) allow HCP Terraform to interact with external systems at specific points in the HCP Terraform run lifecycle. Run tasks are reusable configurations that you can attach to any workspace in an organization.

## Example Usage

```csharp
using Constructs;
using HashiCorp.Cdktf;
/*Provider bindings are generated by running cdktf get.
See https://cdk.tf/provider-generation for more details.*/
using Gen.Providers.Tfe;
class MyConvertedCode : TerraformStack
{
    public MyConvertedCode(Construct scope, string name) : base(scope, name)
    {
        new OrganizationRunTask.OrganizationRunTask(this, "example", new OrganizationRunTaskConfig {
            Description = "An example task",
            Enabled = true,
            Name = "task-name",
            Organization = "org-name",
            Url = "https://external.service.com"
        });
    }
}
```

```terraform
# With write-only HMAC key

variable "hmac_key" {
  description = "The HMAC key for use in the write only field"
}

resource "tfe_organization_run_task" "example" {
  organization        = "org-name"
  url                 = "https://external.service.com"
  name                = "task-name"
  enabled             = true
  description         = "An example task"
  hmac_key_wo         = var.hmac_key
  hmac_key_wo_version = 1
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `Name` (String) Name of the task.
- `Url` (String) URL to send a run task payload.

### Optional

> **NOTE**: [Write-only arguments](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments) are supported in Terraform 1.11 and later.

- `Category` (String) The type of task.
- `Description` (String) A short description of the task.
- `Enabled` (Boolean) Whether the task will be run.
- `HmacKey` (String, Sensitive) HMAC key to verify run task. Conflicts with `HmacKeyWo`.
- `HmacKeyWo` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) HMAC key in write-only mode. Either `HmacKey` or `HmacKeyWo` can be provided, but not both. Must be used with `HmacKeyWoVersion`.
- `HmacKeyWoVersion` (Number) Version of the write-only HMAC key to trigger updates.
- `Organization` (String) Name of the organization. If omitted, organization must be defined in the provider config.

### Read-Only

- `Id` (String) The ID for the Run task.



## Import

Resource tfe_organization_run_task can be imported in the following format: 

```shell
# via <ORGANIZATION NAME>/<TASK NAME>
terraform import tfe_organization_run_task.test my-org-name/task-name
```

<!-- cache-key: cdktf-0.17.0-pre.15 input-5bc576476b8ff4fc818cd27277fe8a5055625f6cf86cf86829516bf52210a5ce -->