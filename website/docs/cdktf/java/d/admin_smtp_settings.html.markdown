---
layout: "tfe"
page_title: "Terraform Enterprise: Data Source tfe_admin_smtp_settings"
description: |-
  Reads SMTP settings for Terraform Enterprise. Only applies to Terraform Enterprise and requires admin token configuration.
---


<!-- Please do not edit this file, it is generated. -->
# Data Source: tfe_admin_smtp_settings

Reads SMTP settings for Terraform Enterprise. Only applies to Terraform Enterprise and requires admin token configuration.

## Example Usage

```java
import software.constructs.*;
import com.hashicorp.cdktf.*;
/*Provider bindings are generated by running cdktf get.
See https://cdk.tf/provider-generation for more details.*/
import gen.providers.tfe.provider.*;
import gen.providers.tfe.dataTfeAdminSmtpSettings.*;
public class MyConvertedCode extends TerraformStack {
    public MyConvertedCode(Construct scope, String name) {
        super(scope, name);
        /*Terraform Variables are not always the best fit for getting inputs in the context of Terraform CDK.
            You can read more about this at https://cdk.tf/variables*/
        TerraformVariable adminToken = TerraformVariable.Builder.create(this, "admin_token")
                .description("An admin access token")
                .build();
        TerraformVariable hostname = TerraformVariable.Builder.create(this, "hostname")
                .default("app.terraform.io")
                .description("The HCP Terraform or Enterprise hostname.")
                .build();
        TerraformVariable token = TerraformVariable.Builder.create(this, "token")
                .description("An access token")
                .build();
        new TfeProvider(this, "tfe", new TfeProviderConfig()
                .hostname(hostname.getStringValue())
                .token(token.getStringValue())
                );
        TfeProvider tfeAdmin = new TfeProvider(this, "tfe_4", new TfeProviderConfig()
                .alias("admin")
                .hostname(hostname.getStringValue())
                .token(adminToken.getStringValue())
                );
        new DataTfeAdminSmtpSettings(this, "foo", new DataTfeAdminSmtpSettingsConfig()
                .provider(tfeAdmin)
                );
    }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `auth` (String) The authentication type. Valid values are `none`, `plain`, and `login`.
- `enabled` (Boolean) Whether SMTP is enabled.
- `host` (String) The hostname of the SMTP server.
- `id` (String) The ID of the SMTP settings. Always `smtp`.
- `port` (Number) The port of the SMTP server.
- `sender` (String) The sender email address.
- `username` (String) The username used to authenticate to the SMTP server.



<!-- cache-key: cdktf-0.17.0-pre.15 input-fe45939b298fcf7a004f9706f249f9075900a05bd3139381e430bac4a4a3fee9 -->