---
layout: "tfe"
page_title: "Terraform Enterprise: Resource tfe_agent_pool_allowed_workspaces"
description: |-
  Adds and removes allowed workspaces on an agent pool.
  ~> Note: This resource requires using the provider with HCP Terraform and a HCP Terraform for Business account. Learn more about HCP Terraform pricing here https://www.hashicorp.com/products/terraform/pricing.
---


<!-- Please do not edit this file, it is generated. -->
# Resource: tfe_agent_pool_allowed_workspaces

Adds and removes allowed workspaces on an agent pool.

~> **Note:** This resource requires using the provider with HCP Terraform and a HCP Terraform for Business account. [Learn more about HCP Terraform pricing here](https://www.hashicorp.com/products/terraform/pricing).

## Example Usage

```java
import software.constructs.*;
import com.hashicorp.cdktf.*;
/*Provider bindings are generated by running cdktf get.
See https://cdk.tf/provider-generation for more details.*/
import gen.providers.tfe.organization.*;
import gen.providers.tfe.workspace.*;
import gen.providers.tfe.agentPool.*;
import gen.providers.tfe.agentPoolAllowedWorkspaces.*;
import gen.providers.tfe.workspaceSettings.*;
public class MyConvertedCode extends TerraformStack {
    public MyConvertedCode(Construct scope, String name) {
        super(scope, name);
        Organization tfeOrganizationTestOrganization = new Organization(this, "test-organization", new OrganizationConfig()
                .email("admin@company.com")
                .name("my-org-name")
                );
        Workspace tfeWorkspaceTestWorkspace = new Workspace(this, "test-workspace", new WorkspaceConfig()
                .name("my-workspace-name")
                .organization(Token.asString(tfeOrganizationTestOrganization.getName()))
                );
        AgentPool tfeAgentPoolTestAgentPool = new AgentPool(this, "test-agent-pool", new AgentPoolConfig()
                .name("my-agent-pool-name")
                .organization(Token.asString(tfeOrganizationTestOrganization.getName()))
                .organizationScoped(false)
                );
        AgentPoolAllowedWorkspaces tfeAgentPoolAllowedWorkspacesAllowed =
        new AgentPoolAllowedWorkspaces(this, "allowed", new AgentPoolAllowedWorkspacesConfig()
                .agentPoolId(Token.asString(tfeAgentPoolTestAgentPool.getId()))
                .allowedWorkspaceIds(List.of(Token.asString(tfeWorkspaceTestWorkspace.getId())))
                );
        new WorkspaceSettings(this, "test-workspace-settings", new WorkspaceSettingsConfig()
                .agentPoolId(Token.asString(tfeAgentPoolAllowedWorkspacesAllowed.getId()))
                .executionMode("agent")
                .workspaceId(Token.asString(tfeWorkspaceTestWorkspace.getId()))
                );
    }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `agentPoolId` (String) The ID of the agent pool.
- `allowedWorkspaceIds` (Set of String) IDs of workspaces to be added as allowed workspaces on the agent pool.

### Read-Only

- `id` (String) The ID of this resource. Do not rely on this value — use `agentPoolId` instead.



## Import

Resource tfe_agent_pool_allowed_workspaces can be imported in the following format: 

```shell
# via <AGENT POOL ID>
terraform import tfe_agent_pool_allowed_workspaces.foobar apool-rW0KoLSlnuNb5adB
```

<!-- cache-key: cdktf-0.17.0-pre.15 input-0772e8a679b075ad7a13a5f63cde22035432ebd92797b013731856baa5561f67 -->