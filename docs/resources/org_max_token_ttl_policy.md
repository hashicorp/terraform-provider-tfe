---
layout: "tfe"
page_title: "Terraform Enterprise: tfe_org_max_token_ttl_policy"
description: |-
  Manages the maximum time-to-live (TTL) policy for API tokens in an organization. When enabled, this policy enforces maximum lifespans for organization, team, audit trail, and user tokens, revoking any tokens that exceed the configured limits.
---

# tfe_org_max_token_ttl_policy

Manages the maximum time-to-live (TTL) policy for API tokens in an organization. When enabled, this policy enforces maximum lifespans for organization, team, audit trail, and user tokens, revoking any tokens that exceed the configured limits.

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `audit_trail_token_max_ttl` (String) Maximum lifespan allowed for audit trail tokens to access the organization's resources. Defaults to two years (2y). Format: <number><unit> where unit is h (hours), d (days), w (weeks), mo (months), or y (years). Decimals are supported (e.g., 0.5h for 30 minutes). Examples: 1h, 2.5d, 3w, 1mo, 2y.
- `org_token_max_ttl` (String) Maximum lifespan allowed for organization tokens to access the organization's resources. Defaults to two years (2y). Format: <number><unit> where unit is h (hours), d (days), w (weeks), mo (months), or y (years). Decimals are supported (e.g., 0.5h for 30 minutes). Examples: 1h, 2.5d, 3w, 1mo, 2y.
- `organization` (String) Name of the organization. If omitted, organization must be defined in the provider config.
- `team_token_max_ttl` (String) Maximum lifespan allowed for team tokens to access the organization's resources. Defaults to two years (2y). Format: <number><unit> where unit is h (hours), d (days), w (weeks), mo (months), or y (years). Decimals are supported (e.g., 0.5h for 30 minutes). Examples: 1h, 2.5d, 3w, 1mo, 2y.
- `user_token_max_ttl` (String) Maximum lifespan allowed for user tokens to access the organization's resources. Defaults to two years (2y). Format: <number><unit> where unit is h (hours), d (days), w (weeks), mo (months), or y (years). Decimals are supported (e.g., 0.5h for 30 minutes). Examples: 1h, 2.5d, 3w, 1mo, 2y.

### Read-Only

- `audit_trail_token_max_ttl_ms` (Number) Internal: Exact milliseconds for audit trail token TTL as returned by the API.
- `id` (String) The ID of the token TTL policy (same as the organization name).
- `org_token_max_ttl_ms` (Number) Internal: Exact milliseconds for organization token TTL as returned by the API.
- `team_token_max_ttl_ms` (Number) Internal: Exact milliseconds for team token TTL as returned by the API.
- `user_token_max_ttl_ms` (Number) Internal: Exact milliseconds for user token TTL as returned by the API.


